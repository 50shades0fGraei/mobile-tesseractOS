let pkgs = import (builtins.fetchTarball "https://github.com/NixOS/nixpkgs/archive/nixos-24.05.tar.gz") {}; android-sdk = pkgs.androidenv.composeAndroidPackages { buildToolsVersions = ["35.0.0"]; platformVersions = ["34"]; platformTools = true; cmdlineTools = true; acceptAndroidSdkLicenses = true; }; in pkgs.mkShell { buildInputs = [ pkgs.jdk17 pkgs.gradle android-sdk ]; JAVA_HOME = pkgs.jdk17; ANDROID_HOME = "${android-sdk}/libexec/android-sdk"; }